<script>
  // #54: the Finance surface. Deliberately off the Billing page so it collides with
  // nothing there. Right now it holds the one report the CAO asked for; it is the home
  // for the report-shaped asks whose data already rides the feed (nr2026#259).
  import LoadsByBucket from './LoadsByBucket.svelte'
</script>

<LoadsByBucket />
